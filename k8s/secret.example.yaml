# EXAMPLE ONLY — do NOT commit a real connection string.
# Create the secret at deploy time instead, e.g.:
#
#   kubectl create secret generic payroll-db \
#     --namespace payroll \
#     --from-literal=connection-string="Server=tcp:<sql_server_fqdn>,1433;Initial Catalog=PayrollDB;User ID=<admin>;Password=<password>;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;" \
#     --dry-run=client -o yaml | kubectl apply -f -
#
apiVersion: v1
kind: Secret
metadata:
  name: payroll-db
  namespace: payroll-dev
type: Opaque
stringData:
  connection-string: "Server=tcp:REPLACE.database.windows.net,1433;Initial Catalog=PayrollDB;User ID=REPLACE;Password=REPLACE;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
